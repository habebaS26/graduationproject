import 'package:flutter/material.dart';

class umsScreen extends StatelessWidget {
  const umsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
